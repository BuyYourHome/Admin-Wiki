# Duplicate And Conflict Log

| Item | Status | Notes |
| --- | --- | --- |
| Project Room names | monitor | Similar or legacy rooms should remain separate unless Wes explicitly authorizes consolidation. |
| Purpose text | monitor | Some older READMEs may not use a standard Purpose section; the refresh script uses a neutral fallback instead of guessing. |
| Functional groups | initial design | Group assignments are dashboard navigation choices, not ownership changes. |
| Entity Relationship diagram path | stale supporting reference | `entity-relationship-chart.md` points to a missing Estate Documents SVG; the owning room's existing `outputs\entity-relationship-chart.svg` is used by Dashboard. |
