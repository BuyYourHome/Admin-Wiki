# Dashboard Change List

| ID | Requested change | Status | Implementation boundary |
| --- | --- | --- | --- |
| DASH-001 | Give every Project Room card side panel at least one starter action and support several future actions. | completed | `Open Project Room README` is the truthful default; room-specific actions are appended. |
| DASH-002 | Allow Wes to initiate deletion of an unwanted Project Room from its side panel. | workflow preview implemented | `Review deletion` presents one confirmation without name typing, names only the selected room and its documented matching skill/task, exposes unresolved resources, and downloads an audit-plan record. It does not delete, archive, hide, rename, route, or alter anything. |
| DASH-003 | Show canonical documented modes in a selected-room side-panel combo box. | completed | Modes are extracted from the room README and matching skill when available. Selection is interface-only and activates nothing. |
| DASH-004 | Codify the current grouping and place the future group-change control in the displayed Group property. | completed | Current assignments and group definitions live in `config\project-room-groups.json`. A selection previews another group but is not saved or applied. |
| DASH-005 | Provide a second Quick action position for later assignment. | completed | Rooms with fewer than two actions show a disabled `Future action available` slot. Rooms that already have a README action and a room-specific action do not receive a third slot. |
| DASH-006 | Open Dashboard document actions without replacing the Dashboard page. | completed | Dashboard links use the browser's normal new-tab/window behavior with `target="_blank"`; the browser controls final tab handling. |
| DASH-007 | Assign Gracious Millionaire's available second Quick action to its website. | completed | `Open GraciousMillionaire.com` opens `https://graciousmillionaire.com` in a separate browser tab/window context. |
| DASH-008 | Add a prominent local Dashboard refresh control. | completed | The top-level `Refresh Dashboard` button calls only the local Dashboard refresh endpoint, reloads data after success, and shows a truthful failure message when the local refresh server is unavailable. |
| DASH-009 | Show a visible Project Room card badge when Wes's approval or confirmation is explicitly needed. | completed | Dashboard reads only valid explicit entries from `config\project-room-attention.json`; `Confirmation needed` and `Approval needed` use distinct badges. Empty or invalid entries show no alert. |
| DASH-010 | Make it obvious that the deletion dialog can be dismissed without acting. | completed | The dialog now has an X close control and a `Close dialog` action. Both use the non-destructive dialog-cancel path and do not create a plan or alter a Project Room. |
| DASH-011 | Add a prominent `Ask Jean` control. | completed | The Dashboard opens the supported `codex://threads/<thread-id>` link for the active Jean's Voice task. It does not claim to start a voice session automatically. |
| DASH-012 | Add an SOP selector to the SOPs Project Room side panel. | completed | The selector is generated from `Project Rooms\SOPs\outputs\SOP Index.md`; it enables viewing only where a matching clean SOP page exists, and opens that page separately. |
| DASH-013 | Show Jean's portrait in the top panel just left of the Dashboard titles. | completed | The local Dashboard topbar now uses the provided Jean portrait from `site\assets\jean-wright-topbar.png`, positioned to the left of the eyebrow and title text. |
| DASH-014 | Reduce the height of the top count strip and move functional side-panel controls ahead of metadata. | completed | The summary strip now uses a compressed metric layout, and the selected-room panel now shows Mode, SOP actions, Quick actions, and Project Room controls above the Group, Status, and Skill metadata block. |
| DASH-015 | Make the count strip even smaller and add SOP group filtering with documented pages only. | completed | The metric strip now places each count to the right of its label, and the SOPs side panel now shows only canonical Markdown-backed SOP pages with a group filter above the SOP selector. |
| DASH-016 | Host the Dashboard on the private LAN from WesStudio without exposing arbitrary repo files or remote write actions. | completed | Added a dedicated LAN read-only host, install/remove scripts, remote-client action restrictions, firewall/task documentation, and a path allowlist that serves only approved Dashboard views. |
| DASH-017 | Remove the visible blank PowerShell window from normal LAN host startup. | completed | The scheduled-task design now starts the LAN host through `Start-DashboardLanHostHidden.vbs`, so normal sign-in startup can run without leaving a visible PowerShell console window. |
| DASH-018 | Keep the deletion preview available on WesStudio local LAN-host access while leaving remote LAN clients read-only. | completed | The LAN-host view now leaves deletion preview enabled for local access on WesStudio, while remote LAN clients still have deletion review disabled. |
| DASH-019 | Scope the two SOP selector combo boxes to the SOPs Project Room only. | completed | General room metadata now renders as read-only text, and only the SOPs Project Room keeps the SOP group and SOP selection combo boxes. |
| DASH-020 | Keep `Documented mode` and `Group` selectors for every Project Room while leaving only the SOP selectors scoped to SOPs. | completed | Restored the general `Documented mode` and `Group` selectors for every room and kept only the SOP group/SOP selectors scoped to the SOPs Project Room. |
| DASH-021 | Detect documented modes without requiring `Mode` in the section title. | completed | The Dashboard parser now treats a heading as a mode when its section explicitly begins with `Use this mode ...`, so names such as `Diagram` appear in the `Documented mode` selector without renaming the heading. |
| DASH-022 | Make the Dashboard search box filter Project Rooms reliably on mobile Chrome. | completed | The search box now listens to `input`, `search`, `change`, and `keyup` so mobile-browser search input updates the visible Project Room cards reliably. |
| DASH-023 | Keep the side panel hidden until a Project Room is selected so mobile opens on the cards first. | completed | The Dashboard no longer auto-selects the first room on load, and the detail panel hides again when filtering removes the selected room from the visible result set. |
| DASH-024 | Invoke a distinct Dashboard action when a documented mode is selected. | completed | Mode actions are now keyed in `config\dashboard-actions.json` by exact room and mode, and the LAN host serves only explicitly configured safe internal targets rather than arbitrary repo files. |
| DASH-025 | Establish a Dashboard `Mode Map` mode that helps translate plain-English requests into correct mode mappings. | completed | The Dashboard now supports keyed `modePanels` with several side-panel helper controls, and `Dashboard` -> `Mode Map` shows the canonical single-action and multi-control mapping patterns. |
| DASH-026 | Show open Manager tasks inside `Manager` -> `Tasks` and allow local status changes from the Dashboard. | completed | Dashboard refresh now parses the canonical Manager task register, the side panel shows open tasks with visible status, and only the full local Dashboard can write a status change back to `Project Rooms\Manager\working\task-register.md`. |
