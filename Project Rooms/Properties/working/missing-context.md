# Missing Context

| Question | Why It Matters | Status |
|---|---|---|
| What is the project goal? | Needed before selecting sources or drafting outputs. | missing |
| What is the expected final deliverable? | Determines whether outputs should be a memo, chart, document, spreadsheet, workflow, or other artifact. | missing |
| What sources control the answer? | Prevents unsupported facts or stale materials from being blended into the work. | missing |
| City, state, ZIP, and county for 5512 Desert Willow Ln | Needed to complete `Profit!E2` and `Profit!G2` without guessing. | missing |
| Whether the Tensity working copy is acceptable for production templates | Wes allowed Tensity for this test, but it has not been approved as the default clean template. | open |
| Whether inherited `#REF!` matches should be fixed before using this workbook | Verification found `Gnatt Chart!AP14` and `Paint!J46` formula-error matches outside the cleaned Profit inputs. | open |
| Standard folder/file package for a production property setup | Needed to know whether this PR should create only the project-management workbook or additional standard folders/files for each property. | open |
| Whether offer-stage properties should be tracked with active/closed properties | Teams has an `Offers` folder with project-management workbooks, but Wes has not said whether the authoritative ledger should include offer-stage prospects. | open |
| How closed-property archive values should be represented | Teams has `X_Closed Properties`; a separate survey pass is needed before closed properties are merged into the live ledger. | open |
| Final value precedence when workbook values conflict with signed documents | Needed to decide whether signed leases, insurance declarations, settlement statements, or project-management workbook values control when amounts differ. | open |
