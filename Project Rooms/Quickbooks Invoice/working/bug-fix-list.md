# Quickbooks Invoice Bug-Fix List

Keep these defects for future implementation and regression testing. Recording an item here does not authorize a global messaging, email, browser, or skill-deployment change.

| ID | Defect | Required Behavior | Status / Evidence |
| --- | --- | --- | --- |
| `QB-001` | Intuit sign-in page was treated as a blocker without attempting Chrome's retained credential. | Attempt the one recognized retained-account flow before declaring a blocker; stop only for a manual password, MFA, CAPTCHA, recovery, identity ambiguity, or failed attempt. | Canonical Quickbooks Invoice rule corrected in commit `04113ca3`; installed-copy synchronization and cross-machine deployment remain separate. |
| `QB-002` | Browser reporting said QuickBooks “redirected” to sign-in although only an existing tab was inspected. | Distinguish inspection, navigation, redirect, click, retry, and confirmed page result in task and durable outcome reporting. | Observed on Poyner message `prmsg-invoice-entry-quickbooks-poyner-1277608-20260901-001`. |
| `PRM-001` | `Needs Wes` was visible only in the destination task and central queue. | On a new actionable blocker, send Wes one verified proactive notification containing the exact task, message, blocker, and required action; surface send or verification failure rather than remaining silent. | Poyner retry sent a verified OfficeAssist blocker email on `2026-09-01T22:51:08Z`. Global rule not yet implemented. |
| `PRM-002` | The messaging manager makes `Needs Wes` terminal and cannot resume it. | Use a resumable waiting state and transition the same message ID, payload hash, authorization, and destination back to `Processing` after Wes clears the blocker. Do not require a new durable message, dispatch, acceptance, or generic “go.” | Current manager rejects `StartProcessing` unless state is `Accepted`. Global fix pending. |
| `PRM-003` | A terminal destination blocker stalls the sending Project Room and can cascade into later workflow failures. | Keep the sending Project Room safely waiting on the same child message; after destination completion, return the result and resume the sending workflow automatically. | Demonstrated by the Poyner Invoice Entry → Quickbooks Invoice handoff. Global fix pending. |
| `PRM-004` | A Wes reply that clears a blocker is not attached to the waiting lifecycle or used to wake the exact destination. | Record the resolution as an event on the same message, wake the exact destination task when needed, and continue from the next safe step without changing scope. | Global fix pending. |
| `SKILL-001` | A committed canonical skill correction may remain absent from the installed runtime copy. | Make commit, push, installation/sync, machine coverage, and new-session activation status explicit so a rule is not mistaken for deployed behavior. | Quickbooks Invoice canonical rule committed; installed copy was not synced during the rule-only request. |
