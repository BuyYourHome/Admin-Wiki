# WES-VIDEOEDITOR Continuous Synthetic Validation Setup

**Superseded setup:** the self-test below exhausted its one-attempt limit without a recipient receipt. Do not use its old filter or retry it. Current local setup is [[Project Rooms/Create PR/outputs/WES-VIDEOEDITOR Distinct Recipient Transport Validation]], authorized by `prmsg-jean-wve-distinct-recipient-validation-design-20260905-001`. Preserve the installed 24/7/no-expiry schedule and replace only the persistent exact-ID filter as described there. The remainder of this file is historical instruction/evidence, not the current test procedure.

Date: 2026-09-05
Status: current authorized setup instructions prepared; remote override **not installed** from WESSTUDIO.

The existing filename is retained for continuity. Its former one-hour title and expiry requirements are superseded by decision 003.

## Exact Authority And Target

- Current decision: `prmsg-jean-wve-synthetic-hours-exception-20260905-003`.
- Decision SHA-256: `f5b42edf2a47ef5c28e6b38e0cd80cd414f1999adf29a22bd7f19ebe7e0d294b`.
- Decision destination: Create PR / `019fdc5e-a1da-7e10-b388-a3be3830ac89` / WESSTUDIO. The WVE owner reads this linked authority but must not accept/finalize the Create PR-addressed decision.
- Existing owner: `PR Messaging Dispatcher - WES-VIDEOEDITOR`, task `01a05d0c-8031-7d92-9474-ab2330008ddb`.
- Existing automation: `pr-messaging-dispatcher-wes-videoeditor`.
- Only synthetic: `prmsg-create-pr-wve-dispatcher-recipient-validation-20260905-001`.
- Synthetic SHA-256: `3920b0a0251234448410c1763bc8c33dab8370278e71ef29629b79974d4a89fe`.
- Authorized schedule: **every five minutes continuously, with no office-hours/weekend gate and no expiry, until Wes closes the hours**.

Decision 003 supersedes only the hours, today-only, expiry, and automatic-restoration prerequisites from decisions 001/002 for this exact synthetic setup. The synthetic's older weekday/earliest-window wording is superseded by linked authority, not edited. All exact-ID, one-attempt, no-production, and no-promotion restrictions remain.

## Preserved History And Publication Correction

- Decision 001 required independent automatic restoration; WVE reported that gate blocked activation.
- Decision 002 allowed later restoration but required a hard one-hour schedule expiry. WVE safely pulled `d731ad5a`; its isolated scheduler test found an overdue heartbeat may launch after expiry, so it did not activate the validation override. Reported recovery evidence remains outside Git at `C:\Users\IRAMa\AppData\Local\BuyYourHome\PRMessaging\recovery\synthetic-hard-expiry-preflight-20260905T175930Z\setup-status.json`.
- These failure records are historical, not current setup gates. Do not repeat expiry testing or add a new mechanism to satisfy a superseded requirement.
- Prior decision 002's Blocked publication result remains immutable history. Jean published commits `49f0989b`, `ca3bb33f`, `e314cb89`, and `d731ad5a`; GitHub main at `d731ad5afe0d4510e67068aeb498954726f87b6c` was independently verified. Decision 003 records this publication correction.

## Local Setup Only

1. Use the existing owner task on WES-VIDEOEDITOR under its intended normal Windows identity. Verify Get-Location, exact machine/task, and the canonical `C:\Codex\Wiki Files` project on main. Use explicit shell workdir and absolute file paths. Safely pull the published correction through normal Git rules; do not stash, reset, force, or overwrite unrelated work. No Teams-synced working repo.
2. Read this report, current Messaging Rule, dispatcher skill/heartbeat prompt, manifest, and the exact decision/synthetic records through the canonical manager. Verify hashes and identities. Before setup require the synthetic is Queued with zero attempts/notifications, no receipt/result, and max attempts one. If it changed, reconcile rather than reset/retrigger.
3. Use the supported local automation tool to inspect the existing automation. Preserve its ID, name, kind, target, model/reasoning, and notification policy. Capture the original prompt/schedule/status in profile-local recovery state outside Git. Do not edit app storage directly, add a task/automation/service/watchdog, or change another machine's schedule.
4. Configure that existing heartbeat to run every five minutes continuously with no expiry, until Wes closes hours. No hard-expiry or automatic-restoration capability is required for this override. Record actual installation/activation only after read-back; do not invent an activation timestamp.
5. Store a short pointer prompt that rereads this report, current canonical rules, manifest, and exact central records each scheduled run. Include the literal synthetic ID. The **exact MessageId filter persists across every scheduled turn** of this override; it is not a one-turn diagnostic and must never disappear on the next heartbeat. The specific linked decision overrides only the canonical hours restriction, not other safety rules.
6. Read back and verify the existing automation's preserved fields, continuous five-minute schedule, no expiry, exact-filter pointer, and intended task. Keep the manifest `validation_ready` and `dispatchable: false`.
7. This setup turn must not invoke the helper, claim, write StartAttempt, notify the synthetic recipient, write its receipt/lifecycle, or force a heartbeat. Return installed/blocked, saved-original reference, read-back evidence, and current synthetic state to Create PR via a linked canonical status. Manual setup is not unattended-validation evidence.

## Scheduled Test Contract

Only normal scheduled turns of the existing heartbeat may run the test. Each run reconciles the exact central ID/hash, task/machine, manifest, current attempts/receipt/result, and one-attempt limit. Immediately before the helper, require the literal nonblank exact MessageId. Missing/wrong/terminal/already-attempted target is never permission to choose another record.

The only authorized helper command, for a **scheduled turn, never setup**, is:

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File 'C:\Codex\Wiki Files\tools\pr-messaging\Claim-ProjectRoomDispatch.ps1' -ActorTaskId '01a05d0c-8031-7d92-9474-ab2330008ddb' -MessageId 'prmsg-create-pr-wve-dispatcher-recipient-validation-20260905-001'
```

Use approved normal-Windows-identity execution for saved SMB access. This wrapper does not change machine execution policy. Never use the unfiltered command from the normal heartbeat prompt during this override. Respect the deterministic helper's exact selection and bounded result; do not bypass an explained skip.

After one claim, issue one supported safe notification to the exact local task. The dispatcher is also the recipient: do not fabricate receipt/completion from the polling or setup turn, interrupt active work, substitute another task/runtime, or confuse successful enqueue with authoritative acceptance. The receiving turn must verify the immutable record and write its own Accepted, Processing, and final result. Follow bounded canonical reconciliation for ambiguity/unsupported self-notification. Never reset the budget or create a replacement test.

After a terminal outcome or exhausted attempt, make no further claims and never fall back to production. Report completion/blocker once; remain quiet on unchanged state. The open-hours setting may remain until Wes closes it, but removing the synthetic restriction, reverting to unfiltered processing, or enabling production requires separate authority. Do not auto-restore an unfiltered prompt after the test.

No business, mailbox, email, browser, financial, ACL/credential, readiness-promotion, other-machine schedule, or implementation action is authorized.

## Future Low-Token Intent

Wes's clarification that the heartbeat can remain 24/7 once the new model is in place describes the intended permanent low-token dispatcher architecture. It does not authorize implementing that design in Create PR, changing the current task's model, removing the synthetic-only restriction, promoting production, or bypassing the remaining readiness gates.

## Short WVE Paste Prompt

```text
In this existing WES-VIDEOEDITOR dispatcher task 01a05d0c-8031-7d92-9474-ab2330008ddb, safely pull the published correction into C:\Codex\Wiki Files on main and read C:\Codex\Wiki Files\Project Rooms\Create PR\outputs\WES-VIDEOEDITOR One-Hour Synthetic Exception Setup.md. Verify Get-Location first.

Reconcile decision prmsg-jean-wve-synthetic-hours-exception-20260905-003, hash f5b42edf2a47ef5c28e6b38e0cd80cd414f1999adf29a22bd7f19ebe7e0d294b. Preserve existing automation pr-messaging-dispatcher-wes-videoeditor and its identity/model/notification settings; back up its original prompt/schedule/status outside Git. Configure only this heartbeat every five minutes continuously with no expiry until Wes closes hours, retaining the report's exact synthetic MessageId filter across ALL scheduled turns.

Keep the synthetic unchanged, max attempts one, and dispatchable false. If already attempted/terminal, reconcile—never reset. This setup turn must not run the helper, claim, StartAttempt, notify the test, write its lifecycle, or force a run. Only normal scheduled turns may perform the exact filtered test. After terminal/exhausted state, no further claims or production fallback; report once and stay quiet on unchanged state. Return local setup/read-back evidence or the exact blocker. No production, promotion, model change, or low-token implementation here.
```

## Completion Boundary

Create PR changes only scoped instructions and decision metadata. The central final result records actual commit/publication evidence. WESSTUDIO does not install the remote override or claim test success. WVE must use the supported local tool and supply its own setup evidence; the synthetic remains unchanged and readiness remains unproven until the actual gated lifecycle passes.
