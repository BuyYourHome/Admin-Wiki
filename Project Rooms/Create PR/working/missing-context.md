# Missing Context

| Question | Status | Notes |
| --- | --- | --- |
| Should Create PR have an automation? | not needed | Current request is on-demand only. |
| Should every future PR be added to Admin Home? | case by case | Add when the room should be discoverable from the wiki start page. |
| Should future PR work get a branch? | answered | No. Work from `main` unless Wes explicitly asks for a branch. |
| Should dedicated task creation block PR setup? | answered | No. Commit local setup first, try task creation once, and leave thread id pending if the connector does not return promptly. |
| Should relationship diagrams be linked globally? | case by case | Save diagram outputs in Create PR first; add or copy to a global index only after Wes approves that placement. |
