# Duplicate And Conflict Log

| Item | Status | Notes |
| --- | --- | --- |
| STR vendor tab design | unclear | STR does not currently match the standard two-group vendor-tab layout. Do not insert STR invoice records until Wes approves the target design. |
| Atlantic Discount Flooring LLC invoice `001199` for Outrigger | inserted | Read-only inspection found no duplicate evidence. After Wes authorized finishing processing, inserted into `Flooring!A8:H8` and validated the row total at `$4,629.65`. |
| GTI Stone Design Corporation invoice `101492` for Outrigger | routed to Review | Read-only workbook inspection found no hits for invoice number `101492`, vendor name, exact amount `$4,563.00`, or `Borris`. Routed to `Review!A2:K2` because countertops plus sinks could map to `Cabinets` or `Plumbing Fixtures`; Doc Scan also noted possible source-file uncertainty with `26-03-17 Borris invoice.pdf`. |
| Outrigger `Gnatt Chart` formulas | existing workbook issue | Read-only inspection found existing `#REF!` formulas in later `Gnatt Chart` timeline columns. Direct Flooring summary link is `Gnatt Chart!G14 = +Flooring!I22`, but downstream validation after insertion should account for the pre-existing formula issue. |
| Lowes PRO BYH 5997 statement closing `2026-03-17` for Outrigger | item-level packet consumed | Initial partial Plumbing Fixtures insertion, all-lines Review-first reprocess, and revised project-first packet processing were superseded. Consumed the item-level Doc Scan packet and placed only high-confidence Outrigger packet rows 1-12 into the Outrigger `Review` table. Destination Worksheet is blank for packet rows 1-3 pending vendor-tab/allocation review and `Plumbing Fixtures` for packet rows 4-12. Packet rows 13-25 were intentionally left out of Outrigger because they are Home/non-project, unclear-project, mixed-tab/project-review, PO-conflicted, or accounting-review rows. |
| Sullivan Surveying, PLLC invoice `2395` for 908 Pond | placement review needed | SharePoint resolved the exact workbook `Property/26_Project Management - 908 Pond St 3.xlsm`. Read-only workbook inspection found no real duplicate evidence for vendor `Sullivan Surveying`, invoice number `2395`, source filename `sqinv-Walter-Brothers-2395.pdf`, or customer labels `Walter Brothers` / `Glasgow Design`. The only `2395` hit was an internal formula-object artifact, not invoice text. Insertion is held because surveying/property due diligence does not map to an approved Vendor Tabs Mode tab or documented Pond expense area. |
| Tim Fleming Pond hours, 2026-07-06 through 2026-07-17 | no likely duplicate; placement held | Tim Fleming confirmed 22 hours at `$62.50/hour` for `$1,375.00` by replying `Correct`; no invoice number was provided. Project-room search found no prior Tim Fleming packet or processing log for this Pond hours item beyond this packet. Read-only workbook scan found existing old `Tim` sheet Tim Flemming contract/payment rows but no matching Pond hours invoice in `Review` or obvious duplicate entry for this confirmed invoice. Insertion is held because destination worksheet/placement is still unknown. |
