interface:
  display_name: "Invoice Entry"
  short_description: "Process invoices, collected-money receipts, Time Cards, and project records."
  default_prompt: "Use the Invoice Entry workflow to process a structured invoice, collected-money Receipt, statement, or Time Card packet, validate source and routing, prevent duplicates, and perform only authorized document or project-spreadsheet actions."
