interface:
  display_name: "Invoice Entry"
  short_description: "Process invoices, Time Cards, and project expense records."
  default_prompt: "Use the Invoice Entry workflow to process a structured invoice, statement, or Time Card packet, validate source and routing, prevent duplicates, and perform only authorized invoice or project-spreadsheet actions."
